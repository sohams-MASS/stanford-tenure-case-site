# Stanford tenure case — report

Static build of `results/strangelove.html` from a private analysis repository.
Published automatically by that repository's CI. Sources, data, and method
live in the private repo; every number on the page carries its provenance.
